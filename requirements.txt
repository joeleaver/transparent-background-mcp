# Core MCP dependencies
mcp>=1.2.0

# Essential image processing (always needed)
pillow>=10.0.0
numpy>=1.24.0

# Utilities
pydantic>=2.0.0
psutil>=5.9.0
tqdm>=4.65.0

# Model-specific dependencies (installed on-demand by dependency manager)
# torch>=2.0.0
# torchvision>=0.15.0
# ultralytics>=8.3.0
# transparent-background>=1.3.4
# rembg>=2.0.67
# huggingface-hub>=0.20.0
# transformers>=4.30.0
# opencv-python>=4.8.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
mypy>=1.5.0
pre-commit>=3.0.0
